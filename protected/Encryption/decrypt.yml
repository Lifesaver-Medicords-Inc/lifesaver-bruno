info:
  name: decrypt
  type: http
  seq: 1

http:
  method: GET
  url: http://{{host}}:{{port}}/LSMI/decrypt?data
  params:
    - name: data
      value: ""
      type: query
  auth:
    type: basic
    username: admin
    password: asdmin

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
