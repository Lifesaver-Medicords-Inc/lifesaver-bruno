info:
  name: stream_patient_side
  type: http
  seq: 3

http:
  method: GET
  url: http://{{host}}:{{port}}/LSMI/api/telemed/availability/stream/patient?hospital_id=1&department_id=1
  params:
    - name: hospital_id
      value: "1"
      type: query
    - name: department_id
      value: "1"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
