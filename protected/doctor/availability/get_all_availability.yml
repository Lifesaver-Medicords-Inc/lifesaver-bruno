info:
  name: get_all_availability
  type: http
  seq: 4

http:
  method: GET
  url: http://{{host}}:{{port}}/LSMI/api/telemed/availability/
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
