info:
  name: set_availability
  type: http
  seq: 1

http:
  method: POST
  url: http://{{host}}:{{port}}/LSMI/api/telemed/availability
  body:
    type: json
    data: |-
      {
          "doctor_id": 6,
          "name": "Patrick",
          "hospital_id": 1,
          "department_id": 1,
          "is_active": true
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
