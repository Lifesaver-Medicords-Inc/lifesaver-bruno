info:
  name: flush_key
  type: http
  seq: 3

http:
  method: DELETE
  url: http://{{host}}:{{port}}/LSMI/api/admin/redis/key?key=queue:hospital:1:dept:1
  params:
    - name: key
      value: queue:hospital:1:dept:1
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
