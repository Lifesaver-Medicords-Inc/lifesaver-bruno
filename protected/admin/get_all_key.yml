info:
  name: get_all_key
  type: http
  seq: 2

http:
  method: GET
  url: http://{{host}}:{{port}}/LSMI/api/admin/redis/keys
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
