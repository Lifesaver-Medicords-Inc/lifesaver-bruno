info:
  name: health
  type: http
  seq: 2

http:
  method: GET
  url: http://{{host}}:{{port}}/LSMI/health
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
