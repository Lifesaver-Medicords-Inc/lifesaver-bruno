info:
  name: Flush All
  type: http
  seq: 5

http:
  method: DELETE
  url: http://{{host}}:{{port}}/LSMI/api/admin/redis/flush
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
